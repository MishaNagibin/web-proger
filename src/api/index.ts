import categories from "@/api/categories"
import categoriesFrontend from "@/api/categoriesFrontend"
import courses from "@/api/courses"

export default {
    categories,
    categoriesFrontend,
    courses,
}